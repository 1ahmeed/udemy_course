/*
Dismissible(
     key:UniqueKey (),
     onDismissed: ( direction){
        AppCubit.get(context)!.deleteData(id: models['id'].toString());
     },)
 */

/*
{
          return Dismissible(key: UniqueKey(),
            background: Container(
              color: Colors.red,
              child: Row(
                mainAxisAlignment: MainAxisAlignment.start,
                children: [
                  Expanded(child: Icon(Icons.delete)),
                  Icon(Icons.delete),

                ],
              ),
            ),
            onDismissed: (direction){
            tasks.deleteData(id:)
            },
            child:buildTasksItem(tasks[index],context,) , );
        },
 */

/*
getName().then((value){
            //   if(value is FutureOr) {
            //     print("success");
            //     throw("error");
            //   }
            // }).catchError((error) =>
              print(error.toString()) );
 */

/*

validator: (value) {
        if (value!.isEmpty) {
          return ('Email is wrong');
        }
        return null;
      },
      onFieldSubmitted:on(value) {
        print(value);
      },
      onChanged: (value) {
        print(value);
      },
 */


/*

6-learn model
7-تغير رؤيه الباس
 */

